<!-- SPDX-License-Identifier: MIT -->

# Install and uninstall

For normal installation, use exactly one of these supported paths:

- **Steam Workshop:** subscribe to ZombieBuddy 2.3.3 or newer and Living Fellows, then enable both for the save.
- **Standalone Windows release:** extract the release ZIP, close Project Zomboid, and run `Install.bat`. Run `Uninstall.bat` to remove it and restore the original launcher configuration.
- **Standalone with the in-game Debug menu (playtesters):** run `Install-Debug.bat` instead of `Install.bat`. It installs the same build with the in-game Debug tab and manual companion spawn enabled. It is a playtest configuration, not for public or streamed play. The same `Uninstall.bat` removes it and restores the original launcher.

Do not enable Workshop and standalone copies together. See the main [README](README.md) for the complete new-user guide, non-standard Steam library instructions, save backup guidance, and troubleshooting.

## Maintainer native playtest

Version 0.21.2 includes an original `SCNativeCompanion extends IsoPlayer` implementation. The companion is an NPC, uses reserved internal index 3 for APIs that require an index, and is never inserted into the four static local-player slots. Construction is deferred until the Lua-to-Java request has unwound, and `OnCreateLivingCharacter` callbacks are muted only around the owned NPC constructor before being restored exactly. Updates run through Build 42's generic character loop, bypassing the local-player controller and its input, event, singleton and camera side effects. The bridge rejects multiplayer, split-screen, occupied extra player slots, unsafe squares, missing native components, and any local-player state mutation.

The installer does not install Java, alter JVM arguments, instrument classes, or modify `projectzomboid.jar`. It does update `ProjectZomboid64.json`: it appends an external owned JAR to the classpath and changes the configured main class to `SCLauncher`. A uniquely named copy of the original JSON is retained under `build\game-config-backups` for audit and rollback. The launcher initializes the bridge and delegates to the original game main class.

It refuses to run if Project Zomboid is open or if the legacy loose file `zombie\characters\IsoSurvivor.class` exists under the selected game root.

After all automated gates pass, install with:

```powershell
.\scripts\Install-Local.ps1 -NativeBridge
```

The installed private copy enables the debug-only tab, including explicit household spawn controls. It does not create companions automatically every minute. Source and Workshop staging retain normal encounter and faction production rules. Animation, combat, inventory, vehicles, death/Knox/corpse/reanimation, save/load, base patrol, native construction, Base Life logistics, quarantine, and infection-crisis outcomes still require live playtesting.

For 0.16.0, use Debug > Spawn household near a loaded residential area. Confirm one to three visible residents appear, remain absent from the companion roster and right-click team commands, and begin real timed barricade work on secondary doors or windows while keeping one primary entry. Approach from outside and cross the two warning bands; verify speech comes from a resident, ordinary proximity does not cause an immediate attack, and persistent trespass or aiming escalates. Deliver the displayed request, verify its reward was unavailable in ordinary stock, then barter at 125% value while Tolerated and 100% while Trusted. On a disposable sandbox, test theft, injury, the cooling-off timer, full-value restitution, permanent murder hostility, save/reload without duplicates, and hibernation/wake outside 120/inside 100 tiles. The Factions and Debug panels must remain translucent and scroll correctly at the smallest supported window size.

For 0.17.0, leave a discovered two- or three-person household active through a full in-game day. Verify that residents visibly rotate among watch, meals, maintenance, organizing, socializing, reading, and sleep without animation loops, and that only one representative approaches an acceptable visitor near the entrance. The Factions tab must show a stable personality, relationships, current routines, qualitative supplies, any active crisis, and rumor progress. In the debug-only build, select all six personality profiles; trigger and resolve Supply collapse, Illness, and Internal dispute; advance routines; audit supplies; and reveal a rumor. The rumor must appear as an `[LF]` annotation on the vanilla world map and survive save/reload in the disposable test save. Confirm every panel and response remains English.

For 0.18.0, meet the representative outside a discovered household and verify that right-click shows the household conversation only within speaking range. Ask about needs, residents, trade, dangers, and rumors; responses and overhead speech must come from the representative. Accept exactly one supply, medical, or local-threat contract, save/reload, then complete or explicitly withdraw it. Confirm promises expire, remembered help improves future terms, aimed weapons block entry, invitations permit temporary trespass and safe rest, and theft or hostility revokes access. Exercise a reserve refusal and an under-value offer; the UI must show the refused categories and numeric counteroffer. In the debug-only build, force all three contract types and Hidden severity, Diverted delivery, Rival objection, Broken reward, and Private dissent. Verify contested invitations can be respected or appealed, unpaid rewards leave visible household debt, and first completion gives only future recruitment consideration—not direct recruitment. All text must remain English, translucent, wrapped, and scrollable.

For 0.18.1, accept each contract type and confirm its orange `[LF Contract]` annotation appears exactly once and disappears on completion, withdrawal, expiry, patient death, or household destruction. For medical help, carry two clean ripped sheets and alcohol wipes instead of the exact original items; the progress rows must report inventory availability before the real transaction moves the items to the representative. Save and reload midway through all three types. Check delivery and kill notifications, twelve-hour and three-hour warnings, minimum loaded-area enforcement for threat reports, two-click withdrawal confirmation, explicit reserve quantities/reasons, lowered-weapon access, trespass revocation, and safe-rest readiness only while invited and inside the house. Resize to the smallest supported window and verify all English text remains wrapped, translucent, scrollable, and clear of the feedback footer.

For 0.21.2, first load a copy of a schema-1 save and complete two save/reload cycles. Verify every nested bag, worn/attached item, hand item, weapon attachment, fluid mixture, condition, and personal keepsake returns once in the same hierarchy. Change each Living Fellows sandbox option and verify it takes effect without deleting an existing companion or household when a limit is lowered. With a trusted household, complete the required contracts, recruit one nonessential resident into a trial, then verify both return-home and permanent-join outcomes across save/reload. Confirm only recruited companions receive a red minimap dot and first-name label. Ask every conversation topic repeatedly and verify bounded English line variation, correct named context, personality flavor, factual consistency and speech anchoring. Short overhead lines should remain fully readable for at least eight real-time seconds and longer lines for up to fifteen seconds before their normal fade, without duplicates; a new line must replace the old line normally, including while game time is accelerated. In a disposable save, kill one recruited companion and verify named, non-duplicating grief, varied safe mourning, danger interruption, save/reload and long-term decay for both teammates and any origin household. With two discovered households, force each Faction World event in Debug and verify relation/news persistence plus bounded word-of-mouth standing. Establish a camp, classify storage, assign several roles, and verify readiness, watch rotation, workload, maintenance, and ambient downtime survive reload. Open Support: it must report bridge protocol `42.20-isocompanion-5`, copy a readable report, and explain a deliberately missing or mismatched bridge without spawning an unsafe fallback. Exercise a recoverable test circuit and confirm Retry failed subsystems closes it. Workshop-path testing must use ZombieBuddy 2.3.3 or newer; local launcher testing remains a separate development path.

For 0.21.3, stop outdoors with a nearby recruited companion on Follow and confirm no calm downtime begins. Repeat indoors, allow reading, repair, crafting, washing or sitting to start, then walk away. The visual action must cancel before follow movement begins, without sliding or retaining a kneeling pose. Debug output should name the activity in a bounded `[SurvivorCompanion][downtime]` start, finish or cancel line. An unrelated native timed action must likewise hold movement until its queue and pose clear.

For 0.21.4, press Collapse on each dock side. The expanded panel must be hidden and replaced by exactly one compact LF launcher without immediately reopening. Clicking LF or pressing F7 restores the saved expanded panel; pressing F7 again returns to the launcher. Dragging the launcher, changing resolution, and reloading while collapsed must keep it fully on-screen and must never create a duplicate or partial panel.

For 0.21.5, save while the LF launcher is visible, quit completely, and load the save again. The launcher must be visible immediately when gameplay starts, without pressing F7. It must still open on its first click and preserve its dock and vertical position.

For 0.21.6, enable scavenging and let a companion approach a container or zombie corpse from several tiles away. Its native path must stop before Loot starts; the complete rummage animation must finish before exactly one item transfers, and movement may resume only afterwards. Repeat with an immediately adjacent container and with several useful containers nearby.

For 0.21.7, repeat that route with a worn backpack and watch both the roster and Overview. They must progress through approach, Loot and verification, then name the exact item and destination. Confirm the source loses one object and the backpack gains that same object. During separate attempts, create danger, issue a new order, remove the selected item, fill the inventory, and quit before Loot completes; each attempt must leave the item in exactly one valid location and must not commit late after reload. Let two companions target one container, verify only one owns it, then change the contents of a previously unhelpful container and confirm it becomes eligible immediately. Finally watch packing, base-storage deposit, ground drop and build-supply retrieval: every inventory effect must wait for its full Loot animation. Lead a companion far enough to exercise native recovery and then enter a house: the full player model must remain visible, the old path must not resume, and the companion must pause once to check both sides of a normal doorway before continuing.

For 0.22.1, use the debug build and repeat ordinary follow, combat, scavenging and faction-house tests with 1, 4, 8 and 16 active companions. Open Debug > AI performance profiler and use Refresh after each scenario. Ordinary play should keep frame p95 close to the 2 ms mod budget; short emergency spikes may occur, but sustained overload must raise the load level and slow background work without delaying combat, retreat, health or vehicle safety. A distant route, full perception sweep, container search and production household search must visibly continue after yielding instead of restarting. Scrolling the debug panel must remain stable because profiler values refresh only on the explicit button. Copy the report and verify it lists top systems, top companions, yielded jobs, cache hits/misses and p50/p95/max timing. Then test Copy player while crouching, walking, running and holding formation. Complete and interrupt bandaging, emergency cloth tearing, scavenging, clothing changes, downtime and camp work; no result may commit before the animation finishes, and safe completion should sometimes create a short interruptible think/look pause.

For 0.22.2, lead multiple companions through doors, fences, stairs, vegetation and narrow furniture gaps. Native path requests may wait or turn, but must not be restarted as false stuck movement. A second companion must queue outside a short choke instead of entering it with the first. Repeat interactions with a container, sink, patient, storage object and construction square when one side is blocked; the companion must select another reachable working position. Reuse and then block a route to verify short-lived success and failure memory without permanent blacklisting. Finally isolate one zombie, let a companion shove it down and confirm a safe follow-up stomp; a second close attacker, excessive distance or a standing target must cancel the stomp.

For 0.22.3, lead four or more companions through one door and one stair. The first actor must retain the whole native transition; later ordinary requests queue by priority and waiting order without stealing it. Create a head-to-head narrow-aisle conflict and verify one companion receives the explicit yield request, chooses one safe side, and does not oscillate; if no lateral square exists, bounded deadlock recovery must eventually clear the route. Exercise Loot, Bandage, Craft, sitting, climbing and an unrelated vanilla action while Follow is active: ordinary locomotion must stop without translating the pose, while genuine survival interruption uses rollback. In Debug, select the actor, refresh the 30-second Movement recorder, and verify its target, next square, state owner, native path, queue, blocker and recovery. Copy and Clear must affect only that companion. Public/Workshop staging must retain `movementRecorderEnabled = false`.

For 0.22.4, select Copy player, Walk and Sneak in turn, then order Retreat or trigger a genuine combat overrun. On clear ground the companion must turn and use native running regardless of the selected ordinary movement policy. A controlled one-zombie fighting withdrawal may backstep or strafe while keeping the threat visible; an encircled or overrun companion must not remain in that slower stance. Lead the escape through a door, fence, stair, broken-window transition and an unavoidable bush corridor: only the constrained edge should walk, with running resuming after it clears. In the private Debug movement report, verify requested/effective speed and the override reason. A persistent wall-collision animation should cancel stale movement ownership, and any native actor state that fails to clear must produce a bounded `actor_state_timeout` diagnostic instead of waiting forever.

The protected live sandbox defaults to a five-minute outer timeout because a heavily modded seed can spend several minutes loading scripts, maps and assets before Build 42 exposes its click-to-start screen. The in-game harness remains separately bounded and exits the disposable client when it completes.

For 0.15.11, put a companion on Stay, Guard, Base duty, or nearby Follow inside a building with open curtains. Under Stealth, wait through safe downtime and verify the companion sometimes reserves an open curtain, sneaks up to five tiles to it, closes it only after arriving, and never reopens it. Ordinary daytime behavior may rarely open or close a curtain, while nighttime should close one. A visible zombie, player danger, or a new order must cancel an approach immediately. Multiple companions must not crowd one curtain or repeatedly reverse it.

For 0.15.12, set Combat doctrine to Stealth or Weapon priority to Quiet and lead a companion past a visible zombie with open space for an alternate route. The companion should sneak or take a visibly wider arc instead of cutting close enough to attract it. If the zombie moves, the route should refresh without rapid flicker. If attacked, trapped, or ordered to retreat, survival movement must override the preferred margin.

For 0.15.8, walk followers around both ends and the open leaf of doors, locked/obstructed gates, parked cars and trailers, moved furniture, full-square player construction, narrow aisles, stair tops/bottoms, slopes, trees, shrubs, zombies, animals, and other companions. A routine route should prefer vegetation clearance but use a tree or bush when it is the only escape. A collision must produce one `navigation-blocker` entry with type, object, square, actor state, and recovery, then choose a different edge until the blacklist expires. Climbing, knockdown, wall-collision, and unfinished-action states should wait rather than fight the animation. Test open, locked, barricaded, invincible, smashed-with-glass, smashed-without-glass, thumpable, and empty-frame windows separately.

For 0.15.7, lead an armed follower through open ground while one visible zombie closes from the front. During a safe break-contact move, its torso and weapon should remain toward the zombie while its feet use backward or diagonal player steps; a lateral escape edge should use a side-step. Add a second close attacker or force a blind corner, doorway, stair, tree, or shrub edge: the follower must stop strafing, turn, and cross normally rather than back into the obstacle. Test an overgrown house with a cuttable bush on the only usable exit route. The route must remain valid, but the bush edge must be walked under native steering before tactical retreat resumes outside. With safe separation and clear Rules of Engagement, verify one covering shot or emergency shove can be followed by continued retreat; encirclement, unsafe range, hold fire, Stealth, or a friendly in the firing lane must prevent the shot.

For 0.15.6, recruit ten followers and use a four-seat vehicle. With `Ride with the player` enabled, exactly three followers should receive passenger seats; the other seven must remain alive in the roster, stop trying to enter once capacity is allocated, and never teleport after the moving vehicle. If another passenger takes a reserved seat, the manifest must reallocate the remaining free seats. After the player exits, seated followers should exit beside their own doors and overflow followers should resume ordinary follow movement. Toggle one Rules of Engagement selector and verify every recruited companion, plus one later recruit, shows the same doctrine. Stealth fights only an immediate attack; Close Defense removes threats within roughly five tiles of a team member; Ranged Support prefers firearms and visible targets; Weapons Free attacks visible nearby threats more freely. In a passenger seat, Stealth and Close Defense must never shoot. Ranged Support may fire only through an open or broken side window at no more than 15 km/h, and Weapons Free at no more than 30 km/h. Closed windows, friendly fire lanes, excessive speed, no ammunition, unsafe range, and retreat pressure must block fire.

For 0.15.5, recruit two followers and park a four-seat car with both passenger doors reachable. Enter while they are close: each should deliberately approach a free passenger door, stop, enter one distinct non-driver seat, stay in the roster without taking damage, and remain seated during a drive of at least 100 tiles. Exit the car and verify both companions reappear on safe door-side squares, retain their inventory/health/orders, restore normal on-foot animation, and resume follow. Repeat while entering before one distant follower arrives: the car must not pull or teleport that follower into motion; after you stop and exit, the unloaded follower should safely rejoin. The log must contain no `living native companion has no current world square`, health-gate removal, or vehicle-seat error from these checks. Then repeat the 0.15.4 tree check below.

For 0.15.4, lead a follower along roads with trees on one or both verges, past isolated roadside trees, and through a narrow but genuinely walkable tree-lined gap. It should keep visible clearance from trunks. If it does collide, it must stop within roughly one second, step away from the tree, and resume toward a nearby safe formation slot rather than continue running in place. Then repeat the 0.15.3 door and armor checks below.

For 0.15.3, first walk a follower repeatedly through single and double doors. A door must remain open while any companion occupies the threshold, close after the actor clears it, and a stalled follower must stop and sidestep rather than keep running into the same edge. Give the follower a bulletproof vest both at inventory root and inside its worn backpack; when safe, it should recognize the item as clothing, remove only conflicting weaker clothing, equip the vest, and retain the backpack. Then continue the 0.15.2 movement, animation, scavenging, downtime, stress, persistence, and community checks below.

For 0.15.2, walk an armed follower through open ground, dense shrubs, a dark building, blind corners, rooms, and stairs. It should prefer a short clear detour around bushes, keep its feet moving without sliding, pause only once at deliberate checks, ready the equipped weapon while visibility is poor, and lower it after returning to clear ground or a threat-free base. Verify loot, reading, clothing, washing, crafting, treatment, and bandage replacement each show a plausible player animation on the companion rather than on the local player. During scavenging, confirm that the item moves only after rummaging finishes, the companion never moves while kneeling, and a failed container is not checked continuously. Also keep two recruited survivors safely inside the camp for several in-game hours. Confirm that idle movement has a destination instead of a tight circle; Overview thoughts and boredom update without visible menu flicker; only the correct companion speaks; and a supply-run request offers four working replies. Use debug controls to raise one survivor's stress and verify a single bounded response. A shutdown survivor must seek a quiet indoor square, sit on the floor, remain responsive to danger, stand before moving, and recover sooner after reassurance. Bottle and furniture outbursts must never happen during combat, must not damage the base object, and must not consume anything except the verified carried empty bottle. Save and reload after thoughts, a shared event, and a supply promise to confirm that community state returns once without duplicating an active episode.

For 0.14.7, recruit a follower and walk a route with at least two viable ways around a building or obstacle. It should choose a reasonable route without rapid repathing, favor a safer and less crowded option when costs differ, and still close distance to the player. Stop in formation long enough to see an occasional rear check followed by restored forward facing. Enter several rooms through doors and blind thresholds: the companion should pause once, check left and right, and continue rather than loop at the doorway. Nearby danger must cancel these deliberate observations and keep survival movement responsive.

For the next playtest, verify one companion in each persistent role (follow, stay/idle, guard/patrol, and craft), save and reload, then confirm the roles survive. Let a guard complete a patrol, sit/read, and later patrol again; confirm it stands before moving. Walk a follower through a house with a blind 90-degree turn and a staircase: it should check once, keep facing the concealed landing while stepping, and wait rather than stack on another actor. Enter through one door, move deep indoors, then surround the companion from three close directions; it should break contact toward a safe exterior route or backtrack toward the known entrance without oscillating. Also complete the barricade, save/load, vehicle, permanent-death, corpse, Knox, and reanimation checks below.

For 0.13.1 specifically, compare several fellows to confirm that personality creates visible but conservative preferences; ask "What are you hoping for?" before and after sufficient trust; complete one objective from a real action and verify that it rewards only once; open/refresh the Journal without revealing anything; move the keepsake manually and confirm automation does not consume it; save it in a carried bag and verify it returns once at root after reload; and load one old 0.13.0 companion without duplicate items, memories, objectives, or changed relationship values.

For 0.14.6, first verify that a companion appears, let it scan and manage inventory for at least two minutes, and confirm hotbar/equip actions work without `ReturnValues.put`, `hasTag(String)`, timed-action, navigation argument, or native-update isolation errors. Trigger a zombie warning and hand signal and confirm only the companion speaks/crouches while the camera and player controls remain unchanged. Test the travel-relative formation, stationary aim-turn hold, corridor right-of-way, conversation ring, one-shot gesture, stress posture, and danger interruption in `docs\0.14.6-MOVEMENT-POSITIONING-PLAN.md`. Recruit one companion, move far enough to unload its prior area, and confirm it safely rejoins near the player. Then establish a camp through the world context menu, draw each zone type, classify at least one general and one destination container, assign all five roles, and verify jobs resume after save/load without duplicate items. Queue a wall frame and confirm the companion fetches exact materials and uses the native construction action. Test guard interruption and return, dirty-bandage replacement, quarantine movement and duty restrictions. Finally, test one witnessed and one concealed bite. Lethal crisis outcomes must remain inert until the second explicit confirmation after the safety delay; verify the real attack, corpse and reanimation path on a disposable test save only.

The installer refuses unmanaged targets and records exact ownership/hashes. Before replacing a prior managed install, it moves that rollback outside the live Project Zomboid mod-discovery tree (by default to `build/local-install-backups`). This prevents two folders with the same mod ID from being loaded together. To uninstall and restore that backup transactionally:

```powershell
.\scripts\Uninstall-Local.ps1
```

Stop Project Zomboid before install or uninstall. Workshop users must enable ZombieBuddy 2.3.3 or newer; Workshop cannot apply this project's local development launcher configuration.
